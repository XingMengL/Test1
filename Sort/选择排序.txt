# include <stdio.h>


void SelectSort(int* arrry[], int size)
{
	int i = 0;
	int j = 
	int maxPos = 0;//标记最大元素
	for(i; i < size; i++)
	{
		for(j = 1; j <size - i; j++)
		{
		 	if(array[maxPos] < array[j])
		 	maxPos = j;
		}
		if(maxPos != size - i)
		{
			Swap()
		}
				}		
		
	 } 
	


}

int main()
{
	
	
	return 0;
}